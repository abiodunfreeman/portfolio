# Server-only: leave blank to use Frosty's built-in demo responses.
# Add the real key locally in .env.local or in Vercel's environment settings.
# Never prefix this secret with VITE_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
