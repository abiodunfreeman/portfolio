# Spring Boot API origin (without /api). Copy to .env.local for local integration.
# An empty value runs the explicitly labeled browser demo with local sample data.
VITE_API_URL=http://localhost:8080
